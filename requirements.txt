# Classroom Scheduler - Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
pulp>=2.7.0
pandas>=2.0.0
numpy>=1.24.0

# Excel export support
openpyxl>=3.1.2

# HiGHS solver (faster MIP solver, optional but recommended)
highspy>=1.5.0

# OR-Tools CP-SAT solver (fast constraint programming solver)
ortools>=9.8

#python -m pip install -r requirements.txt
#pip install -r requirements.txt